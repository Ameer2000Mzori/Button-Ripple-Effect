// selecting our elements
var effectButton = document.getElementsByClassName("effect-Button")[0];
// our functions
var fireEffect = function () {
    console.log("start of project");
};
// our eventlisnters
effectButton.addEventListener("click", fireEffect);
